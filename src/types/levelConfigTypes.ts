interface LevelConfig {
  gridColsRows: string;
  background: string;
  pathColor: string;
  wallsColor: string;
  startArea: string;
  finishArea: string;
  startIcon: string;
  finishIcon: string;
  audioSource: string;
}

export default LevelConfig;
